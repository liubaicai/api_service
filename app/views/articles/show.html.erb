
<div class="page-header">
    <h2><%= @article.title %></h2>
    <span>发表于<%= @article.created_at.to_s(:db) %> <%= link_to '编辑', edit_article_path(@article) %></span>
</div>
<div class="bc-body-content row">
    <%= raw @article.text %>
</div>